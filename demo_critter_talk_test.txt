# Reproduces a crash using Talk mode on Bragit (critter type_idx=26 /
# object type 0x5a, tile (17,7) on the chargen-spawn level -- same
# critter as demo_critter_orbit_cardinal.txt).
#
# Pixel coordinates below were read directly off a real screenshot
# taken from this exact camera spot (SETPLAYERPOS 18.500 7.500 768
# 270 0, the same "E" waypoint demo_critter_orbit_cardinal.txt found
# gives the cleanest view of this critter) -- NOT guessed:
#   - Talk mode icon (topmost of the 5 mode-select icons, per
#     PTR_FUN_000858c8_table's comment in uw.c: mode 5 = Talk):
#     window coords (32,72). Confirmed via a 6x zoomed crop of the
#     icon column -- lips/mouth icon, 2nd from top (crosshair/options
#     icon is 1st).
#   - Bragit's on-screen body (torso, green tunic): window coords
#     (422,195). Note: since the walk-in-place fix (222edbf/7c3cd0b),
#     Bragit actively cycles direction/animation even while not
#     translating tiles yet, so this exact spot is only confirmed
#     against this file's own screenshot sequence -- if a future run
#     shows him elsewhere, re-screenshot before assuming the click
#     coords are still right.
#
# Sequence: select Talk mode (SDLCLICK, left button, the mode-select
# button), then right-click Bragit in the 3D view (SDLRCLICK) --
# right-click is how object/NPC interact dispatches in this game (see
# right-click-interact memory / FUN_0003f420's comment), and with
# g_cursor_mode==5 that dispatches to interact_talk_npc.
#
# Run with:
#   UW_ENABLE_3D_GEOMETRY=1 UW_FAST_SLEEP=1 UW_DATA_DIR="$(pwd)/data" \
#     UW_DEMO_FILE=demo_critter_talk_test.txt ./build/uw_dbg

ENTER
ENTER
ENTER
ENTER
ENTER
ENTER
ENTER
ENTER
ENTER
ENTER
ENTER
ENTER
ENTER
ENTER
ENTER
ENTER
ENTER
ENTER
TYPE Avatar
ENTER
ENTER
ENTER
ENTER
SETPLAYERPOS 18.500 7.500 768 270 0
WAIT 5
REVEAL
WAIT 3
REVEAL
WAIT 3
SCREENSHOT /tmp/talktest_before.bmp
SDLCLICK 32 72
WAIT 3
SCREENSHOT /tmp/talktest_mode_selected.bmp
SDLRCLICK 422 195
WAIT 5
SCREENSHOT /tmp/talktest_after.bmp
WAIT 10
SCREENSHOT /tmp/talktest_after2.bmp
