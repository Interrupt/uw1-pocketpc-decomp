UW1 PORT -- OBJECT RENDERING IN THE 3D DUNGEON VIEW
(branch `objects`. Companion to tile-rendering-findings.txt.)

STATUS: not rendering yet, but the path is traced end to end. The object pass
runs, builds real geometry records, they survive the near-clip -- and then
rasterise with a null texture because the object geom-record counter is out
of sync with the render list's record index. Details below under
"WHY THE BILLBOARDS ARE INVISIBLE".

═══════════════════════════════════════════════════════════════════════════
WHERE OBJECTS WOULD RENDER
═══════════════════════════════════════════════════════════════════════════
render_dungeon_view (was FUN_00012970) draws TILE geometry only -- view
matrix, visibility walk, vertex transform, near-clip, render_visible_tile_list.
There is NO object pass in it.

Objects are meant to be emitted from process_visible_tile_cell (was
FUN_0005e604), which for each visible tile calls:

    if (getenv("UW_ENABLE_TILE_FEATURES") != NULL) {
      FUN_00065394(puVar23 + 1);       // <-- was hard-disabled
    }

FUN_00065394 = "render this tile's animated features and the objects sitting
on it (doors, switches, bridges, item billboards)". It:
  1. drains a deferred-object queue (DAT_0023b940[...]),
  2. walks the tile's object chain (resolve_object_link(param_1)),
     classifying each object and deferring some to adjacent tiles,
  3. sorts the survivors by depth (FUN_00064ec8 / FUN_00064e3c /
     FUN_00065128 / FUN_0006508c -- insertion/bubble sort on DAT_0023b8c8[]),
  4. for each, computes screen X/Y/Z (DAT_0023b904 / DAT_0023b920 /
     DAT_0023b91c) and a shade byte (DAT_0023bc88), then calls
     FUN_00060aa0(object) to emit the billboard.

FUN_00060aa0 (~uw.c:47775) appends draw opcode 0xae + slot + sprite/pos data
into the draw-command list DAT_00110fc0 (cursor; base DAT_0023aed0, rewound
by draw_command_list_rewind). It also fills parallel slot tables
DAT_0023b676[] / DAT_0023b4f4[] indexed by the billboard counter DAT_0023b830.

═══════════════════════════════════════════════════════════════════════════
FIXED ON THIS BRANCH (commits 73c8b73, 7647424)
═══════════════════════════════════════════════════════════════════════════
- FUN_00065394: two dropped Ghidra call args
    * `FUN_000535fc()` in the deferred loop  -> FUN_000535fc(slot id)
      (defaulted to 0 -> NULL record -> FUN_00065210 NULL deref)
    * `resolve_object_link()` at the loop tail -> resolve_object_link(param_1)
      (object chain was never advanced)
- FUN_0003671c + 3 siblings: `pcVar[-0x13ad30] = c` copy of DAT_0023c698 ->
  DAT_00101968 -- baked global-delta constant, a wild pointer in the
  recompile. Replaced with a bounded indexed strcpy. (Was blocking ASan
  testing of this whole area -- demo_dungeon_room hit it deterministically.)
- DAT_0023b848 / DAT_0023b8c8(+c9) / DAT_0023bb98(+99+9a): Ghidra split
  these object-sort scratch arrays into lone scalars; the indexed
  writes/reads landed on different memory -> EXC_BAD_ACCESS the instant
  UW_ENABLE_TILE_FEATURES was set. Now backed with real `static` arrays +
  field #defines (uw.c ~line 3382).

Result: `UW_ENABLE_TILE_FEATURES=1` + demo_dungeon_room runs clean
(regression-clean). ON/OFF screenshots are byte-identical -- see the
"WHY THE BILLBOARDS ARE INVISIBLE" section below for the traced reason
(it is NOT the 0xae draw-command list; class-0 objects render through the
same DAT_000a85d8[] mesh list as tiles).

═══════════════════════════════════════════════════════════════════════════
OPEN / NEXT STEPS
═══════════════════════════════════════════════════════════════════════════
1. Fix the object per-record texture pointer -- see "WHY THE BILLBOARDS ARE
   INVISIBLE". Pin down which counter indexes the DAT_000a85d8[] record
   near_clip_visible_tiles reads, and publish FUN_00040770's decoded sprite
   buffer through g_tile_texptr_emit[] at that index.
2. The 0xae / draw-command list (DAT_0023aed0..DAT_00110fc0) is a SEPARATE
   channel FUN_00060aa0 also feeds (opcodes 0x7a, 0xae, 0xb2, 2). Its
   consumer was not located -- likely only matters for render classes 1-3
   (class 0, the spawn objects incl. the bag, uses the mesh list). Not
   needed for the first objects on screen.
3. Once the texptr is correct, verify object quad screen coords / winding /
   size (class-0 scales by DAT_00202508 x DAT_002022f8 = sprite w x h) and
   painter's-order depth vs tiles.
4. Pre-existing latent bugs in the way of ASan testing this area:
   - set_player_tile_position uw.c:~27199: object_list_unlink on
     DAT_002029cc + DAT_00202080*4 + 2 faults (BUS) from main_menu_loop
     when DAT_00202080 is uninitialised-positive; needs a real range guard.
   The non-ASan build tolerates both; ASan does not.

═══════════════════════════════════════════════════════════════════════════
WHY THE BILLBOARDS ARE INVISIBLE  (traced 2026-09-07)
═══════════════════════════════════════════════════════════════════════════
Instrumented demo_dungeon_room at the natural spawn (UW_ENABLE_TILE_FEATURES=1):

- FUN_00065394 is called for ~75 visible tiles; many have objects (tile
  (16,0) has 4). All objects at spawn are render class 0
  ((&DAT_00202c9a)[type*0xd] & 3 == 0): type 0x80 (the bag), 0xc1-0xd5,
  0x140, 0x177.
- FUN_00060aa0's class-0 branch runs for each: emits opcode 0x7a + screen
  X/Y/Z into DAT_00110fc0, calls FUN_00040770 to decode the sprite, then
  builds a 4-vertex quad into DAT_000a85d8[] and bumps the geom record
  count (DAT_000a85d4 / DAT_0023b83c).
- render_dungeon_view: geom records 70 (tiles only, OFF) -> 84 (ON), and
  near_clip_visible_tiles keeps 70 -> 80. So ~10 object quads DO reach
  render_visible_tile_list.
- render_visible_tile_list rasterises all 80. Screenshot ON == OFF
  byte-for-byte -> the 10 object quads draw NOTHING.

Root cause: the per-record texture pointer. render_visible_tile_list reads
`g_tile_texptr_out[record]` (the side channel added because the in-record
4-byte texture field truncates on 64-bit). process_visible_tile_cell writes
`g_tile_texptr_emit[DAT_0023b83c] = get_texture_page(...)` for tiles.
FUN_00060aa0 / FUN_00040770 do NOT write that side channel for objects, so
object records read a stale/tile texptr (or null) -> null-texture span ->
no pixels.

The obvious fix (publish FUN_00040770's decoded sprite buffer via
g_tile_texptr_emit[DAT_0023b83c]) does NOT work as-is: at FUN_00040770 time
DAT_0023b83c is small (observed 6, 19, 22, 29, 38, 45, 61) while the same
object record shows up in near_clip's list at emit-index 70+. So there are
(at least) two record counters that have diverged:
  DAT_0023b83c  -- bumped by process_visible_tile_cell AND FUN_00060aa0
  DAT_000a85d4  -- the count near_clip_visible_tiles iterates
FUN_00060aa0 keeps them "in sync" with `DAT_0023b83c = DAT_000a85d4 = X+1`
at the end of class 0/1, but the value seen mid-function is not the list
index. Need to pin down exactly which counter indexes the DAT_000a85d8[]
record that near_clip reads, and write g_tile_texptr_emit at THAT index
(and confirm near_clip's emit->out remap carries it).

Once the texptr is correct, also verify: object quad screen coords /
winding / size are sane (the class-0 path scales by DAT_00202508 x
DAT_002022f8 = sprite w x h and a -6/-4 * 0.5 offset), and depth ordering
vs tiles (painter's order -- objects emitted after tiles should draw on
top).

DONE on this branch (commit widening DAT_0023c7a0):
- DAT_0023c7a0 undefined4 -> void*[0x140]. FUN_00040770's Ordinal_1041
  sprite buffer was truncated to 32 bits; the memcpy into it and the
  per-frame free both hit a bogus address.
